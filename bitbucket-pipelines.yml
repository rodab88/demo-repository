#  Template N
image: node:16

image: node:16

pipelines:
  default:
    - parallel:
        - step:
            name: Build and Test
            caches:
              - node
            script:
              - npm install  --force
              - npm audit fix --force
              #- npm test
              -npm-force-resolutions
        
            caches:
              - node
